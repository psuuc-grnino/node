<div class="card">
    <div class="card-header bg-warning">
        <h4 class="mb-0">🔒 Protected Content</h4>
    </div>
    <div class="card-body">
        <div class="alert alert-warning">
            <h5>This page is only accessible to authenticated users!</h5>
            <p>Your authentication status has been verified by our session system.</p>
        </div>
        
        <div class="card mb-3">
            <div class="card-body">
                <h5>Session Information</h5>
                <p><strong>Username:</strong> <%= user.name %></p>
                <p><strong>Role:</strong> <span class="badge bg-info"><%= user.role %></span></p>
                <p>This page demonstrates that only logged-in users can access certain routes.</p>
            </div>
        </div>
        
        <a href="/dashboard" class="btn btn-primary">Back to Dashboard</a>
    </div>
</div>